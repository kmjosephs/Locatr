extends layout

block content
  #banner.page-header
    .row
      .col-md-6.col-sm-12
        h1= title
  .row
    .col-md-6.col-sm-12
      // replace all line breaks with <br/> tags when rendring HTML
      p !{(content).replace(/\n/g, '<br/>')}